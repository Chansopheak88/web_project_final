{
  "name": "exploitx-app",
  "version": "1.0.0",
  "description": "ExploitX - Express + React Full Stack App",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix backend --force",
    "start": "node backend/app.js",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\""
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
